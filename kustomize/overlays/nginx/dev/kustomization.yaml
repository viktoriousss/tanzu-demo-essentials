resources:
- ../../../base/nginx

namespace: development

namePrefix:
  dev-

nameSuffix:
  -v2

patches:
- path: patch-development.yaml
- path: patch-namespace.yaml

configMapGenerator:
- name: nginx-config
  behavior: replace
  env: config.properties
